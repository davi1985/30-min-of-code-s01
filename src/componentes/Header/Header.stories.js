import { Header } from '.';

export default {
  title: 'Componentes/Header',
  component: Header,
};

const Template = () => <Header title={'Example Title'} />;

export const Default = Template.bind({});
